---

title: Search Results

---
<script>
  // create iframe
  // only works with the expected style of request, i.e. a single query parameter in the URI
  var query = window.location.search.substring(1);
  if (query.startsWith("query=")) {
    query = query.replace("query=", "");
    var urlquery = "https://help.openshift.com/customsearch.html?q=" + encodeURI(query) + "&l=devcenter";
    document.write('<iframe src="' + urlquery + '" id="search-iframe">');
  }
</script>
